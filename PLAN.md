# Wait, Did I? — Home Checklist

> Источник правды по продукту. Любое продуктовое/техническое решение
> сверяется с этим документом, в первую очередь с разделом
> «Позиционирование». Активные и завершённые планы реализации —
> в конце файла («История планов реализации»).

## Что это за проект

Мобильное приложение-помощник памяти. Решает бытовую забывчивость:
человек выходит из дома и не помнит — запер ли дверь, выключил ли
плиту, закрыл ли окна. Приложение позволяет за пару секунд отметить
пункт чек-листа и при желании сделать фото-подтверждение, чтобы
спокойно идти дальше.

- **Платформа:** Flutter (Android + iOS).
- **Display name:** `Wait, Did I?` (DEV/STAGE-сборки — с суффиксом окружения).
- **Bundle id / package:** `com.pyf.waitdidi` (prod), `com.pyf.waitdidi.stage`, `com.pyf.waitdidi.dev`.
- **App Store subtitle:** `Home Checklist`.

## Позиционирование — важно для всех решений

Приложение позиционируется как **утилита от забывчивости (memory aid)**,
а НЕ как «лечение тревоги» или медицинский продукт.

Тема граничит с симптомами ОКР (навязчивые перепроверки). Поэтому есть
жёсткие продуктовые ограничения — их нельзя нарушать «ради вовлечённости»:

- НЕ добавлять «Peace of Mind Score», проценты спокойствия, оценки.
- НЕ добавлять стрики, геймификацию, бейджи, «не разорви цепь».
- НЕ поощрять повторный просмотр старых фото (никакой галереи для
  пересматривания, никакого OCR по фото).
- НЕ использовать слова «anxiety» / «OCD» в UI, текстах, описании стора.
- НЕ использовать красный цвет и тревожные сигналы. Максимум «тревоги» —
  мягкий жёлтый «стоит проверить».
- Фото — одноразовое подтверждение, а не предмет для перепроверок. В UI
  явно сообщать: одного снимка достаточно.

Цель — пользователь глянул один раз и успокоился, а не залипает в
приложении. Меньше вовлечённости здесь = лучше для пользователя.

## Тон голоса

Разговорный, человечный, с лёгким выдохом облегчения (его задаёт само
название). Не «Verification complete» → а «Готово, дверь отмечена».
Не «2 pending items» → а «Осталось глянуть 2 пункта».

## Дизайн

- Палитра A «спокойный голубой» — основная (светлая тема):
  - Фон: `#F4F7F9` · Карточка: `#FFFFFF`
  - Акцент: `#5A9BD4` · Готово: `#68C58F` · Внимание (жёлтый): `#E8B860`
  - Текст основной: `#2D3748` · Текст второстепенный: `#718096`
- Тёмная тема — опциональная, для вечерних сценариев. Не приоритет MVP.
- Скругления крупные (карточки ~13–14px). Теней почти нет — вместо них
  тонкая граница 0.5px. Никаких неоновых/ярких цветов.
- Шрифт: Inter или системный. Два-три начертания, без декоративных.
- Иконки: один контурный набор одной толщины (Material outlined /
  tabler / lucide). Не смешивать контурные и заливочные.
- Состояния пункта чек-листа — три, читаются без подписи:
  зелёный кружок с галочкой (отмечено), пунктирный пустой кружок (ещё
  нет), жёлтый с колокольчиком (стоит проверить). Красного состояния нет.
- Микроанимация отметки: короткая (~300–400 мс), мягкая. Без конфетти.

## Скоуп

### MVP 1.0 (текущая цель)
- Чек-листы: создание из готовых пунктов и своих. Стартовые шаблоны
  «Выход из дома», «Перед сном».
- Экран отметки пункта: два пути — «сделать фото» и «просто отметить».
  Оба ставят таймстемп.
- Локальное хранение всего (списки, отметки, фото). Без облака.
- Фото авто-удаляются через 30 дней.
- История проверок — простой список «что и когда», без оценок.
- Одно напоминание по времени (например, ежедневно вечером).
- Блокировка приложения по Face ID / passcode.
- Только светлая тема.
- Монетизации в 1.0 НЕТ.

### 1.1–1.2 (после)
- Геозона: напоминание при выходе из дома (фоновый geofencing).
  Вынесено из 1.0 намеренно — технически капризно, ест батарею.
- Несколько локаций, заметки к проверкам.
- Недельная сводка фактов (без «score»).
- Premium-стена: безлимит списков, геозона, бэкап.

### 2.0+
- Семейный доступ (удалённая проверка для пожилого родителя).
- Облачный бэкап, больше шаблонов.

### Вне скоупа полностью
- Псевдо-AI «определение паттернов тревоги», предиктивные алерты.
- Любая геймификация и оценочные метрики (см. «Позиционирование»).

## Технический стек (предполагаемый)

- Flutter 3.x (проект зафиксирован на FVM 3.41.1, см. CLAUDE.md).
- Локальное хранилище: Hive + flutter_secure_storage (для шифрования).
- Камера: camera / image_picker.
- Уведомления: flutter_local_notifications + timezone.
- Геозона (с 1.1): geolocator + geofence_service + google_maps_flutter.
- Состояние: на усмотрение — flutter_bloc или provider/riverpod.
- Календарь в истории: table_calendar.

Стек не догма — если есть более простое решение под конкретную задачу,
предлагай.

## Принципы при работе над кодом

- MVP-минимализм: не строить впрок. Если фича не в скоупе 1.0 — не делать.
- Приватность по умолчанию: данные не покидают устройство. Любой код,
  отправляющий данные наружу, требует явного обсуждения.
- Стандартные компоненты Flutter везде, где можно (Switch, TextField,
  bottom sheet). Кастомить только карточку пункта и анимацию отметки.
- Перед тем как добавить что-то, что повышает вовлечённость, — свериться
  с разделом «Позиционирование». Для этого продукта это антипаттерн.

---

## История планов реализации

Сюда складываются планы конкретных задач (по workflow из CLAUDE.md):
сначала план до согласования, после выполнения — статус и результат.
Лог фактических изменений по файлам — в `CLAUDE_CHANGES.md`.

### Активные планы

#### Онбординг + главный экран (скелет) — 2026-06-04

Флоу: первый запуск → онбординг (3 шага) → главный экран. Без bottom nav:
список карточек, FAB `+` снизу справа, настройки — шестерёнка справа сверху.

Сделано (скелет, без оформления):
- ✅ `OnboardingService` (`core`) — флаг `onboarding_completed` в
  SharedPreferences; зарегистрирован в `app_di` (pre-login scope).
- ✅ `go_router` redirect-гейт: нет флага → `/onboarding`; «Начать» /
  «Пропустить» ставят флаг и ведут на `/home`. `initialLocation = /home`.
- ✅ Роуты: `/onboarding`, `/home`, `/settings` (+ showcase/example как dev).
- ✅ `OnboardingScreen` — PageView на 3 шага (хук → фото-подтверждение →
  места/контексты), точки-индикатор, «Далее»/«Пропустить»/«Начать».
- ✅ `HomeScreen` — `AppScaffold` + шестерёнка → `/settings`, пустое
  состояние «Пока пусто», FAB `+` (заглушка-снек).
- ✅ `SettingsScreen` — заглушка с рабочим «назад».
- ✅ Все три фичи приведены к структуре `screen/` (screen+form) +
  `cubit/` (cubit+state) + `widgets/`; навигация и логика — в кубитах.
- ✅ Все строки вынесены в локализацию (`onboarding.*`, `home.*`,
  `settings.*` в en-US/ru-RU, доступ через `LocaleKeys.*.tr()`).

Тексты выверены под позиционирование (memory aid, без слов
«тревога/anxiety», без геймификации).

🚧 Дальше (отдельными задачами):
- Оформление онбординга (иллюстрации вместо иконок-плейсхолдеров, анимации).
- Карточки списков на главном + модель/хранилище (Hive) вместо пустого
  состояния.
- Флоу создания списка по `+`.
- Наполнение настроек (тема, уведомления, about).
- Запрос разрешений камеры — по первому реальному использованию.

### Завершённые планы

_(пусто)_
